# Copiar como ".env" y completar con el password real.
# Usado por docker-compose.yml (sustitución ${SUPABASE_DB_PASSWORD}) y por
# deployment/02-secrets.yml para el Secret de Kubernetes -- ambos leen el
# mismo valor, ver Documentation o el README para el host/usuario de Supabase.
SUPABASE_DB_PASSWORD=reemplaza-esto-por-el-password-real-de-supabase
